// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { HttpClient, HttpParams } from '@angular/common/http';
import { Injectable, inject } from '@angular/core';
import { Observable } from 'rxjs';

{{#each apiTypes}}
import type { {{tsName}} } from '../models/{{../featureKebab}}-api.models';
{{/each}}

@Injectable({ providedIn: 'root' })
export class {{featurePascal}}ApiService {
  private readonly http = inject(HttpClient);

{{#each resolved.resolvedApis}}
  {{methodName}}(
{{#if requestType}}
    request: {{requestType.tsName}},
{{/if}}
  ): Observable<{{#if responseType}}{{responseType.tsName}}{{else}}unknown{{/if}}> {
{{#if (eq method "GET")}}
{{#if hasQueryParams}}
    let params = new HttpParams();
{{#each parameters}}
{{#if (eq location "query")}}
    if (request.{{name}} !== undefined && request.{{name}} !== null && request.{{name}} !== '') {
      params = params.set('{{name}}', String(request.{{name}}));
    }
{{/if}}
{{/each}}
    return this.http.get<{{#if responseType}}{{responseType.tsName}}{{else}}unknown{{/if}}>(`{{pathExpression}}`, { params });
{{else}}
    return this.http.get<{{#if responseType}}{{responseType.tsName}}{{else}}unknown{{/if}}>(`{{pathExpression}}`);
{{/if}}
{{else if (eq method "PUT")}}
    return this.http.put<{{#if responseType}}{{responseType.tsName}}{{else}}unknown{{/if}}>(`{{pathExpression}}`, request);
{{else if (eq method "POST")}}
    return this.http.post<{{#if responseType}}{{responseType.tsName}}{{else}}unknown{{/if}}>(`{{pathExpression}}`, request);
{{else if (eq method "PATCH")}}
    return this.http.patch<{{#if responseType}}{{responseType.tsName}}{{else}}unknown{{/if}}>(`{{pathExpression}}`, request);
{{else}}
    return this.http.delete<{{#if responseType}}{{responseType.tsName}}{{else}}unknown{{/if}}>(`{{pathExpression}}`);
{{/if}}
  }

{{/each}}
}
