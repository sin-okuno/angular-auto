// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { Injectable, inject } from '@angular/core';
import { Actions, createEffect, ofType } from '@ngrx/effects';
import { catchError, map, of, switchMap } from 'rxjs';

import { {{featurePascal}}ApiService } from '../services/{{featureKebab}}-api.service';
import * as {{featureCamel}}Mappers from '../mappers/{{featureKebab}}.mapper';
import { {{actionsGroupName}} } from './{{featureKebab}}.actions';

@Injectable()
export class {{featurePascal}}Effects {
  private readonly actions$ = inject(Actions);
  private readonly apiService = inject({{featurePascal}}ApiService);

{{#each effectDefs}}
  readonly {{effectName}} = createEffect(() =>
    this.actions$.pipe(
      ofType({{../actionsGroupName}}.{{triggerCreator}}),
      switchMap((action) =>
        this.apiService.{{apiMethod}}({{requestArgExpr}}).pipe(
          map((response) => {{../actionsGroupName}}.{{successCreator}}({{successArgExpr}})),
          catchError((error: unknown) =>
            of({{../actionsGroupName}}.{{failureCreator}}({{failureArgExpr}})),
          ),
        ),
      ),
    ),
  );

{{/each}}
}
