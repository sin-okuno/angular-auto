// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { createFeatureSelector, createSelector } from '@ngrx/store';

import {
  {{resolved.resolvedStore.featureKey}}FeatureKey,
  type {{resolved.resolvedStore.stateInterfaceName}},
} from './{{featureKebab}}.state';
import type { {{featurePascal}}ViewModel } from '../models/{{featureKebab}}-view-model';

export const select{{featurePascal}}State = createFeatureSelector<{{resolved.resolvedStore.stateInterfaceName}}>(
  {{resolved.resolvedStore.featureKey}}FeatureKey,
);

{{#each resolved.resolvedStore.selectors}}
export const {{name}} = createSelector(
  select{{../featurePascal}}State,
  (state) => state.{{storeField}},
);
{{/each}}

export const select{{featurePascal}}ViewModel = createSelector(
  select{{featurePascal}}State,
  (state): {{featurePascal}}ViewModel => state,
);
