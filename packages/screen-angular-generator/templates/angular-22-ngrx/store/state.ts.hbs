// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
{{#each viewTypes}}
import type { {{tsName}} } from '../models/{{../featureKebab}}-view.models';
{{/each}}
{{#each commonTypes}}
import type { {{tsName}} } from '../models/{{../featureKebab}}-common.models';
{{/each}}

export const {{resolved.resolvedStore.featureKey}}FeatureKey = '{{resolved.resolvedStore.featureKey}}';

export interface {{resolved.resolvedStore.stateInterfaceName}} {
{{#each resolved.resolvedStore.fields}}
  {{name}}: {{tsType}};
{{/each}}
}

export const {{resolved.resolvedStore.initialStateName}}: {{resolved.resolvedStore.stateInterfaceName}} = {
{{#each resolved.resolvedStore.fields}}
  {{name}}: {{initial}},
{{/each}}
};
