// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { ChangeDetectionStrategy, Component, OnInit, inject } from '@angular/core';
import { Store } from '@ngrx/store';

import { {{actionsGroupName}} } from '../../store/{{featureKebab}}.actions';
import { select{{featurePascal}}ViewModel } from '../../store/{{featureKebab}}.selectors';
{{#each containerHandlerImports}}
import type { {{importName}} } from '{{importPath}}';
{{/each}}

@Component({
  selector: '{{component.selector}}',
  standalone: false,
  templateUrl: './{{component.fileName}}.component.html',
  styleUrl: './{{component.fileName}}.component.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class {{component.className}} implements OnInit {
  private readonly store = inject(Store);

  readonly vm$ = this.store.select(select{{featurePascal}}ViewModel);

  ngOnInit(): void {
    this.store.dispatch({{actionsGroupName}}.enterPage());
  }

{{#each containerHandlers}}
  {{handlerName}}(
{{#if payloadTsName}}
    payload: {{payloadTsName}},
{{/if}}
  ): void {
{{#if actionCreator}}
    this.store.dispatch(
      {{../actionsGroupName}}.{{actionCreator}}({{#if dispatchArgExpr}}{{dispatchArgExpr}}{{/if}}),
    );
{{/if}}
  }

{{/each}}
}
