// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import {
  ChangeDetectionStrategy,
  Component,
  EventEmitter,
  Input,
  Output,
{{#if component.ownsForm}}
  OnChanges,
  SimpleChanges,
{{/if}}
} from '@angular/core';
{{#if component.ownsForm}}
import { FormGroup } from '@angular/forms';
{{/if}}

{{#each component.typeImports}}
import type { {{importName}} } from '{{importPath}}';
{{/each}}
{{#if component.formId}}
import { {{component.formFactoryName}}, type {{component.formInterfaceName}} } from '../../forms/{{component.formFileBase}}-form.factory';
{{/if}}

@Component({
  selector: '{{component.selector}}',
  standalone: false,
  templateUrl: './{{component.fileName}}.component.html',
  styleUrl: './{{component.fileName}}.component.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class {{component.className}}{{#if component.ownsForm}} implements OnChanges{{/if}} {
{{#each component.inputs}}
  @Input({{#if required}}{ required: true }{{/if}})
  {{propertyName}}!: {{type.tsName}};

{{/each}}
{{#each component.outputs}}
  @Output()
  readonly {{propertyName}} = new EventEmitter<{{#if payloadType}}{{payloadType.tsName}}{{else}}void{{/if}}>();

{{/each}}
{{#if component.ownsForm}}
  form!: FormGroup<{{component.formInterfaceName}}>;

  ngOnChanges(changes: SimpleChanges): void {
{{#if component.formInitProperty}}
    if (changes['{{component.formInitProperty}}'] && this.{{component.formInitProperty}}) {
      this.form = {{component.formFactoryName}}(this.{{component.formInitProperty}});
    }
{{/if}}
  }

{{#if component.searchOutputProperty}}
  emitSearch(): void {
    this.{{component.searchOutputProperty}}.emit(this.form.getRawValue());
  }
{{/if}}

{{#if component.saveOutputProperty}}
  emitSave(): void {
{{#if component.saveEmitExpr}}
    this.{{component.saveOutputProperty}}.emit({{component.saveEmitExpr}});
{{else}}
    this.{{component.saveOutputProperty}}.emit(this.form.getRawValue());
{{/if}}
  }
{{/if}}
{{/if}}
{{#each component.localState}}
  {{name}}: {{tsType}} = {{initial}};
{{/each}}
}
