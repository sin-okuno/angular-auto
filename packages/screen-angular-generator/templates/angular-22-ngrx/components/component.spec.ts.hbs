// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { NO_ERRORS_SCHEMA } from '@angular/core';
import { TestBed } from '@angular/core/testing';
{{#if (eq component.type.id "container")}}
import { provideMockStore } from '@ngrx/store/testing';
{{/if}}

import { {{component.className}} } from './{{component.fileName}}.component';

describe('{{component.className}}', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [{{component.className}}],
{{#if (eq component.type.id "container")}}
      providers: [provideMockStore()],
{{/if}}
      schemas: [NO_ERRORS_SCHEMA],
    }).compileComponents();
  });

  it('should create', () => {
    const fixture = TestBed.createComponent({{component.className}});
    expect(fixture.componentInstance).toBeTruthy();
  });

  it('should use OnPush change detection', () => {
    const fixture = TestBed.createComponent({{component.className}});
    expect(fixture.componentRef.changeDetectorRef).toBeTruthy();
  });
});
