// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { ChangeDetectionStrategy, Component, EventEmitter, Input, Output } from '@angular/core';

{{#each component.typeImports}}
import type { {{importName}} } from '{{importPath}}';
{{/each}}

@Component({
  selector: '{{component.selector}}',
  standalone: false,
  templateUrl: './{{component.fileName}}.component.html',
  styleUrl: './{{component.fileName}}.component.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class {{component.className}} {
{{#each component.inputs}}
  @Input({{#if required}}{ required: true }{{/if}})
  {{propertyName}}!: {{type.tsName}};

{{/each}}
{{#each component.outputs}}
  @Output()
  readonly {{propertyName}} = new EventEmitter<void>();

{{/each}}

  confirm(): void {
{{#each component.outputs}}
{{#if (eq id "confirmed")}}
    this.{{propertyName}}.emit();
{{/if}}
{{/each}}
  }

  cancel(): void {
{{#each component.outputs}}
{{#if (eq id "cancelled")}}
    this.{{propertyName}}.emit();
{{/if}}
{{/each}}
  }
}
