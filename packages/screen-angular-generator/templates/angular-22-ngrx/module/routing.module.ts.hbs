// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

import { {{featureCamel}}AccessGuard } from './guards/{{featureKebab}}-access.guard';
import { {{featureCamel}}DeactivateGuard } from './guards/{{featureKebab}}-deactivate.guard';
{{#each pageComponents}}
import { {{className}} } from './pages/{{fileName}}/{{fileName}}.component';
{{/each}}

const routes: Routes = [
{{#each pageComponents}}
  {
    path: '',
    component: {{className}},
    canActivate: [{{../featureCamel}}AccessGuard],
    canDeactivate: [{{../featureCamel}}DeactivateGuard],
  },
{{/each}}
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule],
})
export class {{resolved.resolvedRouting.className}} {}
