// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { inject } from '@angular/core';
import { CanActivateFn } from '@angular/router';
import { Store } from '@ngrx/store';
import { map, take } from 'rxjs';

import { select{{featurePascal}}State } from '../store/{{featureKebab}}.selectors';

export const {{featureCamel}}AccessGuard: CanActivateFn = () => {
  const store = inject(Store);
  return store.select(select{{featurePascal}}State).pipe(
    take(1),
    map(() => true),
  );
};
