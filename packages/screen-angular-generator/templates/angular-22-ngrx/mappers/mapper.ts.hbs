// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
{{#each apiTypes}}
import type { {{tsName}} } from '../models/{{../featureKebab}}-api.models';
{{/each}}
{{#each viewTypes}}
import type { {{tsName}} } from '../models/{{../featureKebab}}-view.models';
{{/each}}
{{#each payloadTypes}}
import type { {{tsName}} } from '../models/{{../featureKebab}}-payload.models';
{{/each}}
{{#each commonTypes}}
import type { {{tsName}} } from '../models/{{../featureKebab}}-common.models';
{{/each}}

{{#each resolved.resolvedMappers}}
export function {{functionName}}(input: {{inputType.tsName}}): {{outputType.tsName}} {
  // TODO(spec): implement mapping "{{purpose}}"
  return input as unknown as {{outputType.tsName}};
}

{{/each}}

export function mapHttpError(error: unknown): {{apiErrorTsName}} {
  const message =
    typeof error === 'object' && error !== null && 'message' in error
      ? String((error as { message: unknown }).message)
      : 'Unexpected error';

  return {
    code: 'UNKNOWN',
    message,
    status: 0,
    fieldErrors: [],
  };
}
