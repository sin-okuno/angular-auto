// AUTO-GENERATED FILE.
import { ProductStructureEffects } from './product-structure.effects';

describe('ProductStructureEffects', () => {
  it('should define the effects class', () => {
    expect(ProductStructureEffects).toBeTruthy();
  });

  it('should expose effect members', () => {
    expect(Object.keys(ProductStructureEffects.prototype).length).toBeGreaterThanOrEqual(0);
  });
});
