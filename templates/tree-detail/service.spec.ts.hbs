// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { HttpClientTestingModule, HttpTestingController } from '@angular/common/http/testing';
import { TestBed } from '@angular/core/testing';

import { {{naming.serviceClassName}} } from './{{naming.fileBase}}.service';

describe('{{naming.serviceClassName}}', () => {
  let service: {{naming.serviceClassName}};
  let httpMock: HttpTestingController;

  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [HttpClientTestingModule],
      providers: [{{naming.serviceClassName}}],
    });
    service = TestBed.inject({{naming.serviceClassName}});
    httpMock = TestBed.inject(HttpTestingController);
  });

  afterEach(() => httpMock.verify());

  // The Service only performs HTTP; it never touches the Store.
  it('{{flow.detailMethodName}} should issue a GET request', () => {
    service.{{flow.detailMethodName}}('P1').subscribe();
    const req = httpMock.expectOne('{{flow.detailPathExample}}');
    expect(req.request.method).toBe('GET');
    req.flush({});
  });
});
