// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { NgModule } from '@angular/core';
import { RouterModule, type Routes } from '@angular/router';

import { {{naming.componentClassName}} } from './pages/{{naming.fileBase}}/{{naming.fileBase}}.component';
import { unsavedChangesGuard } from './guards/unsaved-changes.guard';

// A reference (view) permission guard can be attached here via `canActivate`
// using the project's existing permission guard, e.g. canActivate: [viewGuard].
const routes: Routes = [
  {
    path: '',
    component: {{naming.componentClassName}},
    canDeactivate: [unsavedChangesGuard],
  },
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule],
})
export class {{naming.routingModuleClassName}} {}
