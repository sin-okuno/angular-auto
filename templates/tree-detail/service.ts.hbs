// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { HttpClient, HttpParams } from '@angular/common/http';
import { Injectable, inject } from '@angular/core';
import { type Observable } from 'rxjs';

import type { {{join apiTypeNames}} } from '../models/api';

/**
 * HTTP access layer. This class only builds requests and returns raw responses.
 * It never touches the Store, dispatches actions, navigates, or shows dialogs.
 */
@Injectable()
export class {{naming.serviceClassName}} {
  private readonly http = inject(HttpClient);

{{#each apis}}
  /** {{description}} ({{httpMethod}} {{path}}) */
  {{methodName}}(
{{#each pathParams}}
    {{this}}: string,
{{/each}}
{{#if hasRequestType}}
    request: {{requestType}},
{{/if}}
  ): Observable<{{responseType}}> {
    const url = `{{urlExpression}}`;
{{#if isQuery}}
    let params = new HttpParams();
    for (const [key, value] of Object.entries(request as unknown as Record<string, unknown>)) {
      if (value !== null && value !== undefined) {
        params = params.set(key, String(value));
      }
    }
    return this.http.{{lower httpMethod}}<{{responseType}}>(url, { params });
{{else if isBody}}
    return this.http.{{lower httpMethod}}<{{responseType}}>(url, request);
{{else}}
    return this.http.{{lower httpMethod}}<{{responseType}}>(url);
{{/if}}
  }

{{/each}}
}
