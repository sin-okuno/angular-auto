// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReactiveFormsModule } from '@angular/forms';
import { MockStore, provideMockStore } from '@ngrx/store/testing';

import {
  selectCanUpdate,
  selectDetail,
  selectViewModel,
} from '../../store/{{naming.fileBase}}.selectors';
import { {{naming.componentClassName}} } from './{{naming.fileBase}}.component';

describe('{{naming.componentClassName}}', () => {
  let fixture: ComponentFixture<{{naming.componentClassName}}>;
  let component: {{naming.componentClassName}};
  let store: MockStore;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [{{naming.componentClassName}}],
      imports: [ReactiveFormsModule],
      providers: [provideMockStore()],
    }).compileComponents();

    store = TestBed.inject(MockStore);
    store.overrideSelector(selectViewModel, {
      treeNodes: [],
      selectedNodeId: null,
      detail: null,
      loading: false,
      saving: false,
      error: null,
      errorMessage: null,
      concurrentUpdate: false,
      isEmpty: true,
      canView: true,
      canUpdate: true,
      canSave: false,
      showConcurrentUpdate: false,
    });
    store.overrideSelector(selectDetail, null);
    store.overrideSelector(selectCanUpdate, true);

    fixture = TestBed.createComponent({{naming.componentClassName}});
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  // Operation: enterPage — the component is created and initialises its forms.
  it('should create', () => {
    expect(component).toBeTruthy();
  });

  // Operation: search — pressing Search dispatches an action (no direct Service call).
  it('should dispatch on search', () => {
    const dispatchSpy = spyOn(store, 'dispatch');
    component.onSearch();
    expect(dispatchSpy).toHaveBeenCalled();
  });
});
