// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';
import { ReactiveFormsModule } from '@angular/forms';
import { EffectsModule } from '@ngrx/effects';
import { StoreModule } from '@ngrx/store';

import { {{naming.componentClassName}} } from './pages/{{naming.fileBase}}/{{naming.fileBase}}.component';
import { {{naming.effectClassName}} } from './store/{{naming.fileBase}}.effects';
import { {{naming.reducerName}} } from './store/{{naming.fileBase}}.reducer';
import { {{naming.featureKey}}FeatureKey } from './store/{{naming.fileBase}}.state';
import { {{naming.serviceClassName}} } from './services/{{naming.fileBase}}.service';
import { {{naming.routingModuleClassName}} } from './{{naming.fileBase}}-routing.module';

@NgModule({
  declarations: [{{naming.componentClassName}}],
  imports: [
    CommonModule,
    ReactiveFormsModule,
    {{naming.routingModuleClassName}},
    StoreModule.forFeature({{naming.featureKey}}FeatureKey, {{naming.reducerName}}),
    EffectsModule.forFeature([{{naming.effectClassName}}]),
  ],
  providers: [{{naming.serviceClassName}}],
})
export class {{naming.moduleClassName}} {}
