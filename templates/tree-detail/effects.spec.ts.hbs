// AUTO-GENERATED FILE.
// Source: {{header.sourceSpecification}}
// Definition: {{header.screenDefinition}}
// Do not edit this file directly.
import { TestBed } from '@angular/core/testing';
import { provideMockActions } from '@ngrx/effects/testing';
import { type Observable, of } from 'rxjs';

import { PermissionService } from '../../../core/services/permission.service';
import { {{naming.serviceClassName}} } from '../services/{{naming.fileBase}}.service';
import * as {{naming.actionsClassName}} from './{{naming.fileBase}}.actions';
import { {{naming.effectClassName}} } from './{{naming.fileBase}}.effects';

describe('{{naming.effectClassName}}', () => {
  let actions$: Observable<unknown>;
  const serviceMock = {
    loadTree: jasmine.createSpy('loadTree'),
    loadDetail: jasmine.createSpy('loadDetail'),
    updateDetail: jasmine.createSpy('updateDetail'),
  };
  const permissionMock = {
    getPermissions: jasmine.createSpy('getPermissions').and.returnValue(of(['{{viewPermission}}'])),
  };

  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [
        {{naming.effectClassName}},
        provideMockActions(() => actions$),
        { provide: {{naming.serviceClassName}}, useValue: serviceMock },
        { provide: PermissionService, useValue: permissionMock },
      ],
    });
  });

  // Test: test-search — loadTree effect calls the Service and maps to success.
  it('should call the service and emit loadTreeSuccess', (done) => {
    serviceMock.loadTree.and.returnValue(of({ {{flow.treeNodesField}}: [] }));
    actions$ = of({{naming.actionsClassName}}.loadTree({ keyword: '', categoryId: null }));
    const effects = TestBed.inject({{naming.effectClassName}});

    effects.loadTree$.subscribe((action) => {
      expect(serviceMock.loadTree).toHaveBeenCalled();
      expect(action.type).toBe({{naming.actionsClassName}}.loadTreeSuccess.type);
      done();
    });
  });
});
